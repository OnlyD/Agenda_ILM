{% extends 'login.html.twig' %}

{% block body %}

{% if error %}
    <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
{% endif %}

    	<div id="formContainer">
			<form id="login" action="{{ path('login_check') }}" method="post">
				    <input type="text" id="loginEmail" name="_username" value="{{ last_username }}" placeholder="Usuario" />
					<input type="password" id="loginPass" name="_password" placeholder="Contraseña"/>
					<input type="submit" name="submit" value="Login" />
    {#

        If you want to control the URL the user
        is redirected to on success (more details below)
        <input type="hidden" name="_target_path" value="/account" />
    #}
			</form>
		</div>		
		
{% endblock %}
